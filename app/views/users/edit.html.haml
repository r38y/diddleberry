= form_for current_user, html: { class: 'form-inline' } do |f|
  %div.input-append
    = f.text_field :name, autofocus: true, placeholder: 'What should we call you?'
    = f.button "Submit", class: 'btn btn-primary'
